// Firebase Functions entry point (placeholder for future deployment).
//
// The Express app is built by ../src/app.js WITHOUT calling listen(), so it can
// be wrapped directly by Cloud Functions. When you're ready to deploy:
//
//   1. npm i firebase-functions firebase-admin
//   2. Uncomment below and add a firebase.json + .firebaserc
//   3. Move (or reference) backend env vars via `firebase functions:config` or
//      the v2 runtime environment.
//
// import { onRequest } from 'firebase-functions/v2/https';
// import { createApp } from '../src/app.js';
// export const api = onRequest({ cors: true }, createApp());

export {};
